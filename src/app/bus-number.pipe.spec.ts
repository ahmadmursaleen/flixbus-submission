import { BusNumberPipe } from './bus-number.pipe';

describe('BusNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new BusNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
